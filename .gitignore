# Secrets and machine-local overrides
.env
.env.*
!.env.example
!.env.*.example
.extra
.path
.gitconfig.local

# Local histories and state
.*_history
.lesshst
.viminfo
.wget-hsts

# Only track the portable SSH authorization list
.ssh/*
!.ssh/authorized_keys

# Only track portable AI instructions
.claude/*
!.claude/CLAUDE.md

.codex/*
!.codex/AGENTS.md
